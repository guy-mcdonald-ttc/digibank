version: '3'

services:
  teamscale:
    image: 'cqse/teamscale:7.8.0'
    hostname: 'teamscale'
    restart: unless-stopped
    working_dir: /var/teamscale
    volumes:
      - ./teamscale:/var/teamscale
      - ..:/bank
    ports:
      - "8080:8080"

